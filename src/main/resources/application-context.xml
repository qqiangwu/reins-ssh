<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Properties -->
    <context:property-placeholder
            ignore-resource-not-found="true" ignore-unresolvable="true"
            location="classpath*:application.properties, file:///${web.config}"/>
    <!-- End properties -->

    <!-- Import all sub context, remove anyone it you don't need it, say mongo.xml -->
    <import resource="facet/mongo.xml"/>
    <import resource="facet/redis.xml"/>
    <import resource="facet/mysql.xml"/>
    <import resource="facet/session.xml"/>
    <import resource="facet/cache.xml"/>
    <import resource="facet/worker.xml"/>
    <import resource="facet/security.xml"/>

    <!-- 扫描所有类，由spring托管。托管后，实例的初始化，析构，组装，依赖注入，都由spring完成　-->
    <context:component-scan base-package="cn.edu.sjtu.reins.ssh.impl"/>
    <context:component-scan base-package="cn.edu.sjtu.reins.ssh.web"/>
    <context:component-scan base-package="cn.edu.sjtu.reins.ssh.support"/>
</beans>